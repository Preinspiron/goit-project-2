{{#each this}}
  <li class='lastComics-item slide'>
    <img
      class='lastComics-img'
      data-id='{{this.id}}'
      src='{{this.urlImg}}'
      alt='{{this.title}}'
    />
    <div class='lastComics-span'>
      <p class='lastComics-date'>{{this.title}}</p>
      <p class='lastComics-name'>{{this.creators}}</p>
    </div>
  </li>
{{/each}}